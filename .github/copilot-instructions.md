# Bagisto Development Guide

The instructions for this repository live in [`AGENTS.md`](../AGENTS.md) at the repository root, the
single source shared by every coding agent. Read it before changing code: it covers the
architecture, the commands, the code style Pint does not enforce, database compatibility, and the
checks a change must pass.
